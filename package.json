{
  "name": "apex",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:generate": "pnpm --filter @apex/database db:generate",
    "db:check": "pnpm --filter @apex/database db:check",
    "db:migrate": "pnpm --filter @apex/database db:migrate",
    "db:ping": "pnpm --filter @apex/database db:ping",
    "db:seed": "pnpm --filter @apex/database db:seed"
  },
  "devDependencies": {
    "turbo": "^2.11.3",
    "typescript": "catalog:"
  }
}
